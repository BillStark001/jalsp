{
  "name": "jalsp",
  "version": "0.1.0",
  "description": "Just Another Lexical and Syntactic Parser (in JavaScript).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "node dist/playground.js",
    "build": "tsc -p .",
    "playground": "npm run build && npm run start", 
    "test": "jest"
  },
  "author": "Bill Stark",
  "license": "MIT",
  "dependencies": {
    "ts-node": "^8.10.2",
    "ts-replace-all": "^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.6",
    "typescript": "^4.9.4"
  }, 
  "jest": {
    "verbose": true, 
    "testMatch": ["**/*.tests.ts"], 
    "preset": "ts-jest"
  }
}
